# ======================================
# POC Auditoria - Variáveis de Ambiente
# ======================================
#
# Copie este arquivo para .env e ajuste os valores conforme necessário:
#   cp .env.example .env

# ======================================
# PostgreSQL
# ======================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=poc_auditoria

# ======================================
# RabbitMQ
# ======================================
RABBITMQ_USER=rabbitmq
RABBITMQ_PASSWORD=rabbitmq123

# ======================================
# Elasticsearch
# ======================================
# Ajuste a memória conforme necessário
# Mínimo recomendado: -Xms256m -Xmx256m
# Padrão: -Xms512m -Xmx512m
ES_JAVA_OPTS=-Xms512m -Xmx512m

# ======================================
# URLs das APIs (Frontend)
# ======================================
# Nota: Estas são configuradas no nginx.conf do frontend
# e não precisam ser definidas aqui. Estão documentadas
# apenas para referência.
#
# VITE_API_CONTAS=http://localhost:8080
# VITE_API_TRANSACOES=http://localhost:5000
# VITE_API_AUDITORIA=http://localhost:5001

# ======================================
# Notas de Configuração
# ======================================
#
# 1. PostgreSQL:
#    - Os schemas (contas, transacoes) são criados automaticamente via init.sql
#    - Porta padrão: 5432
#
# 2. RabbitMQ:
#    - Management UI disponível em: http://localhost:15672
#    - Porta AMQP: 5672
#    - Porta Management: 15672
#
# 3. Elasticsearch:
#    - Porta padrão: 9200
#    - Modo single-node (adequado para POC)
#    - Security desabilitado para simplificar desenvolvimento
#
# 4. Microserviços:
#    - MS-Contas: http://localhost:8080
#    - MS-Transacoes: http://localhost:5000
#    - MS-Auditoria: http://localhost:5001
#    - Frontend: http://localhost:3000
#
# 5. Credenciais de Login (Hardcoded na aplicação):
#    - Admin: admin / admin123
#    - User: user / user123
